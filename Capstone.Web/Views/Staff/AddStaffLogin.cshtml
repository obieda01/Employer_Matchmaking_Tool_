
@{
    ViewBag.Title = "AddAStudentLogin";
}

<h2>Create A Staff Login</h2>


<form action="UpdateStaffLogin" method="post">
    @{string loggedInUserName = ViewBag.userName; }
    @Html.Hidden("userName", loggedInUserName);

    <div>
        <div class="col-sm-5 col-sm-offset-1">
            <div class="form-group label-floating">
                <label class="control-label">Staff Name</label>
                <input type="text" name="staffName" class="form-control" id="">
            </div>
        </div>
        <div class="col-sm-5 col-sm-offset-1">
            <div class="form-group label-floating">
                <label class="control-label">Staff's User Name</label>
                <input type="text" name="userName" class="form-control" id="">
            </div>
        </div>
    </div>
    <div class="col-sm-10">
        <div class="pull-left">
            <input value="Update" type="submit" class='btn btn-fill btn-success btn-wd btn' id="inputButton" />
        </div>
    </div>

    <input id="userName" name="userName" type="hidden" value="@ViewBag.userName" />;


    @*@using (Html.BeginForm("UpdateStaffLogin", "Staff"))
        {*@
    <table>
        <tr>
            <td>@Html.Label("Staff Name: ")</td>
            <td>@Html.TextBox("staffName")</td>
        </tr>
        <tr>
            <td>@Html.Label("Staff's User Name: ")</td>
            <td>@Html.TextBox("userName")</td>
        </tr>
        <tr>
            <td>@Html.Label("Staff's email address: ")</td>
            <td>@Html.TextBox("emailAddress")</td>
        </tr>
        <tr>
            <td>@Html.Label("Confirm staff's email address: ")</td>
            <td>@Html.TextBox("emailAddress2")</td>
        </tr>
        <tr>
            <td>@Html.Label("Is the staff a admin: ")</td>
            <td>
                @Html.DropDownList("isAdmin", new SelectList(new List<Object>{new { value = "Yes" , text = "Yes" },
                    new { value = "No" , text = "No"}}, "value", "text", 2))
            </td>
        </tr>
        <tr>
            <td>Initial Passwords are set to "Password"</td>
        </tr>
    </table>

    <div class="col-sm-10">
        <div class="pull-left">
            @* <input value="Submit" type="submit" id="inputButton" />*@

            <input value="Submit" type="submit" class='btn btn-fill btn-success btn-wd btn' id="inputButton" />

            @* <input type='button'  id="inputButton" class='btn btn-fill btn-primary btn-wd btn-sm' name='Submit' value='Submit' />*@
        </div>
    </div>
    @*}*@
</form>

